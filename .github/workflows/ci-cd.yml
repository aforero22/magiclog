name: CI/CD Pipeline

on:
  #push:
  #  branches: [ main ]
  workflow_dispatch:

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Repository
      uses: actions/checkout@v2

    - name: Build Docker Image
      run: docker build -t aforero68/app-age:latest .

    - name: Log in to Docker Hub
      uses: docker/login-action@v1
      with:
        username: ${{ secrets.DOCKER_HUB_USERNAME }}
        password: ${{ secrets.DOCKER_HUB_ACCESS_TOKEN }}

    - name: Push Image to Docker Hub
      run: docker push aforero68/app-age:latest

    # A partir de aquí, necesitarás comandos específicos para tu proveedor de servicios en la nube
    - name: Deploy to Cloud Service
      run: |
        # Aquí van los comandos para desplegar en tu servicio en la nube
        # Por ejemplo, si usas ECS, sería algo como 'aws ecs update-service...'
